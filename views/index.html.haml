%article
  %h2
    Table of Contents

  %ol
    - Lesson.published.each do |lesson|
      %li
        = link_to lesson.title, "#lesson-#{lesson.id}"
        %a{href: lesson.basename, target: "_blank"}
          %img{src: 'vendor/i.stack.imgur.com/3H2PQ.png'}

%article
  - Lesson.published.each do |lesson|
    = render_template lesson.basename, locals: { lesson: lesson }
